version: '3'
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    tty: true
    entrypoint: qlot exec ./.qlot/bin/clackup --address 0.0.0.0 --port 3000 app.lisp
